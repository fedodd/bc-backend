.open {
  position: absolute;
  bottom: 0;
  background-color: rgba(238, 54, 36, .9);
  width: 100%;
  padding: 30px 0;
}

.open-title {
  font: $font-service;
  color: white;
  text-align: center;
  width: inherit;
  margin: 0 0 30px;
  text-transform: uppercase;
}
