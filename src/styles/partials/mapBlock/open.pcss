.open {
  background-color: $color-accent;
  width: 100%;
  padding: 65px 0;
}

.open-title {
  font: $font-service;
  color: white;
  text-align: center;
  width: inherit;
  margin: 0 0 60px;
  text-transform: uppercase;
}
