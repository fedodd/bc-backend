.footer {
  padding: 80px 0;
  color: $color-grey;
  font: $font;
}

.footer-holder {
  display: flex;
  justify-content: space-between;
}

.footer-logo {
  width: 116px;
  
  &:hover {
    opacity: .5;
    cursor: pointer;
  }
}

.footer-block {
  display: flex;
}

.footer-join {
  margin-right: 60px;
}

.footer-contacts {
  display: block;
  color: $color-grey;
  font-weight: bold;

  &:hover,
  &:visited {
    color: $color-grey;
  }
}

.footer-projectsWrapper {
  margin-top: 20px;
}

.footer-project {

  &:hover {
    opacity: .5;
    cursor: pointer;
  }

  &.is__lite {
    width: 106px;
    margin-right: 50px;
  }

  &.is__cross {
    width: 60px;
  }
}