.banner-social {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  left: $margin-holder;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 135px;
}