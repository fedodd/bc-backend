.academy {
  position: relative;
  z-index: 5;
  padding: 40px 60px 70px $margin-holder;
  box-sizing: border-box;
  height: 100%;

  @mixin respond-to desk {
    
    padding: 50px 40px 50px 60px;    
  }
}

.academy-title {
  margin: 0;
  padding: 0;
  font: $font-altTitle;
  text-transform: uppercase;
  color: $color-altTitle;

  @mixin respond-to desk {
    font: $font-altTitleSmall;
  }
}

.academy-popup {
  position: relative;
  top: 60px;
}

#academySliderDots,
#academySlider,
#academyPopupBtn {
  &.is__active {
    display: none;
  }
}
